#include <bits/stdc++.h>
#include "include/shape.hpp"
using namespace std;


int main(){
    string path = "../test/input.txt";
    ifstream file(path);
    if (!file.is_open())cout << "File Bermasalah";
    Shape input[1000];
    char c;
    string s;
    file.close();

}